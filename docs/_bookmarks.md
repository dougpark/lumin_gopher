# create a lumin bookmark AI Enrichment Process

- Modify the existing RSS enrichment process and make it more generic so it can be used for both RSS and bookmark enrichment
- expand the existing schema to include a `source` field (e.g., "file_drop", "rss", "manual") to differentiate events
- Use the existing RSS polling function

- The Lumin API will need to be expanded for a more generic approach:
